name: Run Action

on: [push, pull_request]

jobs:
  redis-action:
    runs-on: ubuntu-latest

    name: Run Action
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Use Action
        uses: ./
